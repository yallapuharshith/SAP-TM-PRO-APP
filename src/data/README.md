# Data Layer

This folder stores externalized datasets for questions, metadata, and user progress seeds.

- Keep question banks in JSON files.
- Never store question data inside React components.
- Use a loader service in upcoming sprints for scalable pagination and filtering.
